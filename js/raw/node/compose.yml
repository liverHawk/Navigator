services:
    alpine:
        build:
            context: .
            dockerfile: Dockerfile
        stdin_open: true
        tty: true
        volumes:
            - .:/app/js/raw/node
        ports:
            - "3000:3000"